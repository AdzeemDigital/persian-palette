The HTML and test.js preserve the pre-correction v2.1 release.
The two migration scripts are historical one-time helpers, retained for traceability.
They are not supported build commands. Use npm run build from the project root.
